services:
  - type: web
    name: news-app
    env: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npx serve -s build -p $PORT
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
      - key: REACT_APP_API_KEY
        sync: false