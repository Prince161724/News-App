# Example environment variables
# Do NOT store real secrets in this file. Copy to .env locally and add real values.

# Server-side API key used by the Netlify function (do NOT expose this to client-side code)
NEWS_API_KEY=your_newsapi_key_here

# Optional: If you choose to build the API key into the client (not recommended),
# set REACT_APP_API_KEY before running the build. This will expose the key publicly.
# REACT_APP_API_KEY=your_newsapi_key_here
